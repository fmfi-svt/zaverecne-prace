[project]
name = "zaverecne-prace"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "votr",
]

[tool.uv.sources]
votr = { git = "https://github.com/fmfi-svt/votr.git" }
